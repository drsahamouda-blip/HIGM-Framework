name: My First GitHub Action

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Load My Code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "The GitHub Action is successfully running!"
